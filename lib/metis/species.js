/** Species
 *
 * @module species
 */


/** A species */
export class Species {
    /**
     * A species.
     * @param {string} name
     * @param genotype XXX: to be defined later
     */
    constructor (name, genotype) {
        this.name = name
        this.genotype = genotype
    }
}
